# 更新日志

本文件记录每个版本改了什么。**只写用户能感知的变化**——重构、测试调整、
文档错别字这类不影响使用的改动不占篇幅（要看得细就 `git log`）。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.3.2] — 未发布

### 新增

* ★ **「进度」页：同步/下载**全程看得见在干什么**。

  以前同步 147 首到 iPod，界面在**最长的那一段**（无损档要把每首 FLAC 转成
  ALAC，加上往设备里拷 3.3 GB，实测 6 分钟）**什么都不显示**——进度条停在
  0% 一动不动，看起来就是卡死了。现在：

  * 进度条上方显示**当前阶段**：读取歌单 → 下载到本地 → 读取本地文件 →
    写入 iPod → 把 N MB 刷到设备 → 重建数据库并签名
  * 每一段按**自己**的总数算百分比；不会拿「下载了 3/3」去比「要写 147 个文件」
  * 切不出等份的步骤（刷盘、整库重写）显示**一直在滚的条**，并写明
    「它在滚就说明没卡住」——以前这里画的是停在 0% 的死条
  * 显示已用时间；剩余时间按**当前这一步**的速度算

  左侧导航的「下载」改名「进度」，因为它现在同时管下载和同步。

### 修复

* ★ **「下载好了无法同步」——确认对话框上的按钮写着「开始同步 0 首」。**

  先下载到本地、再点同步，`需要下载` 必然是 0（都已经下好了）。而那个按钮
  显示的是"需要下载几首"，于是恒显示 **「开始同步 0 首」**——看起来像没东西
  可同步，或者像坏了，用户就不会点它。同步作业因此从来没提交过。

  按钮现在显示"要同步几首"，并且把两个数字分开写清楚：
  `要同步 147 首` / `本地已有（不用再下）147 首` / 有需要下载时才写
  `其中需要先下载 N 首`。

* ★ **写数据库失败时不再在设备上留垃圾文件**。

  同步是"先拷文件、再写数据库"两步。以前第二步失败（异常，或者进程被关掉），
  已经拷进去的文件就留在设备上了——而 iPod 的界面只认数据库，于是这些文件
  **既看不见、又白占空间**，下次同步还会重新分配文件名再拷一遍，失败一次多
  一份。实测一台机器上就这样堆到 **294 个孤儿文件、6.4 GB**。

  现在写库失败会把这次拷进去的文件撤掉。已经堆出来的可以用新加的
  `tools/find_orphans.py` 清理（默认只列不删）：

  ```bash
  uv run python tools/find_orphans.py --ipod D:\            # 只看
  uv run python tools/find_orphans.py --ipod D:\ --delete   # 真删
  ```

* ★ **网络请求没有"总时长"上限，能把同步拖死**（这就是"同步 147 首时应用
  卡到未响应"的原因）。

  `urllib.request.urlopen(url, timeout=N)` 里的 `timeout` 是**每次 recv** 的超时，
  不是"这个请求总共最多花多久"。服务器只要慢慢吐数据（响应头分几次发、
  或者每几十秒给一点），每次 recv 都算"没超时"，于是这个 timeout
  **永远不会触发**。

  实测抓到的调用栈（同步 147 首时）：

  ```
  ssl.py     in read          ← 一直在等网络
  http/client.py in _read_status
  ncm/client.py  in cover_bytes
  ncm/downloader.py in download_song
  ```

  它就在等一个迟迟不来的响应，而 socket 超时救不了它。表现是整个应用
  无响应（界面每个请求都跟着排队），Windows 最后判定"未响应"。

  现在读响应时自己掐**墙钟**：音频下载 5 分钟、封面 1 分钟、接口 1 分钟。
  超时就报错跳过，不会无限等。

  > 补充说明：这一次同步**最终是成功的**（147 首全部写入、封面齐全、
  > 播放列表也建好了），只是它爬了 9 分半钟，期间界面完全没反应——
  > 所以看起来像"卡死了"。现在这种长时间静默会变成明确的超时错误。

* **作业卡住时会自动把现场写进日志**（应用不再"无声无息地卡死"）。

  以前作业一旦卡在某个阻塞调用里，日志就停在那儿，完全看不出卡在哪一行。
  现在有看门狗盯着：**60 秒没有任何进展**就把**所有线程的调用栈**打进日志
  文件（并在界面上提示"把日志发给开发者"）。

  这条是给"同步大量无损文件时应用卡到未响应"那次加的——当时翻遍日志只知道
  它停在某一步之前，具体位置查不出来。下次再出现，日志里会直接有答案。

## [0.3.1] — 2026-09-21

### 修复

* ★ **装了新版本之后，歌单页全部显示「未下载」**（本地到底有没有文件判错了）。

  下载记录里存的是**相对路径**（`.ncm/cache/123 歌名.mp3`），而相对路径是按
  **当前工作目录**解析的。工作目录是什么完全看用户从哪儿启动——发行版里就是
  解压出来的那个目录，于是那条路径指向了不存在的地方。

  更要命的是接下来的"自愈"：判定"文件找不到"之后，代码会**把记录删掉**
  （当成脏数据清理）。判据错了就删数据，是不可逆的——实测用户那 7 条记录
  全被判成脏的，而文件好好地躺在缓存目录里。

  现在两件事都改了：
  * 相对路径一律按**数据目录**解析，写入时也存成相对数据目录的形式
    （数据目录搬家也不会指错）；
  * **找不到文件只停止计数，不删记录**——记录里的歌名、艺人、来源是有价值的
    元数据。要看"记录在、文件丢了"有单独的入口，界面分开显示。

* **状态显示把「本地」和「iPod」两件事压成了一个词**，导致一首歌
  "在 iPod 上、本地没留文件"时：
  * 界面上只显示「已同步」，看不出本地那份已经没了；
  * **点「下载到本地」也没反应**——「一键选中所有未下载的」永远选不到它，
    右键菜单里的「下载到本地」也跟着置灰（两个动作共用了同一个判据）。

  现在两个维度各说各的：本地这一维决定"下载"要不要做事，设备这一维决定
  "同步"要不要做事，互不干扰。每行显示两个徽章（本地已有 / 本地没有 +
  iPod 上已有 / iPod 上没有 / 未插 iPod）。

* ★ **设备状态是从状态库"回忆"出来的，不是从 iPod 数据库查的**——这是同一个
  功能的第二处、也是更根本的错。

  判断"这首歌在不在 iPod 上"时，代码遍历的是**状态库里的同步记录**，逐条去
  设备上核对文件位置。方向反了：**设备上有多少歌，取决于我们记过多少条**。

  实测：iPod 里 130 首，状态库里只有 10 条记录 → 另外 120 首在界面上全显示
  "iPod 上没有"，而它们明明在设备里。用户的原话是："这个状态更新难道不是
  应该扫描 iPod 的数据库吗"——对，就该那样。

  现在改成拿歌单里的歌去**问设备库**：设备库是设备自己的数据库，不依赖我们
  记没记过。同一份数据从"认到 1 首"变成"认到 39 首"。

  配套还改了两处同病根的地方：
  * 设备上的曲目**按歌名 + 艺人**认，不再只认文件位置——iPod 上的文件名是
    随机的，数据库被重建过（iTunes 同步、恢复备份、别的工具重导）之后位置
    就变了，明明在设备上的歌会被判成"没同步过"，**下次同步再导一遍，歌就重复**。
    实测 10 条记录里只有 1 条位置对得上，而按名字查有 7 首确实在设备上。
  * 同步时算"歌单该包含哪些歌"，以前也只认状态库里有的记录 → 从命令行、
    旧版本、别的数据目录导进去的歌**永远进不了 iPod 上的歌单**。现在直接用
    设备曲目自己的 ID。

  **只按歌名认是不够的**（这是量了真实数据才发现的）：同一歌名常常是不同的
  录音——歌单里"普通朋友"是宋雨琦、设备上是陶喆，"天天"是刘大拿对陶喆的。
  只比歌名会把翻唱当成同一首，结果是"该同步的歌被判成已在设备上，永远进不去"。

* **没插 iPod 时界面会说谎**：以前这种情况后端返回空集合，等于宣称
  "设备上一首都没有"，于是界面上已经同步过的歌全变成"未同步"，
  让人以为白同步了。现在这种情况显示「未插设备（状态未知）」——
  **不知道就说不知道**，不编一个数字。

* 「已下载 N 首」现在数的是**文件确实还在的**，不是记录条数——
  界面上写 N 首就得是 N 首能播的。记录在、文件丢了的单独报一个数。

* 去掉发行包里的 `启动.bat`。它早期有实际用途（旧版要用户自己装 node，
  脚本负责检查并提示），node 打进包里之后它只剩 `cd` + `start` 两句，
  而这两句都不必要——程序是 GUI 程序（双击不弹黑窗），启动不依赖
  工作目录，数据在 `%APPDATA%`。**直接双击 `ipod_manager.exe` 就是完整用法。**
  实测：工作目录换成 `C:\`、清空所有 `IPOD_MANAGER_*` 环境变量，照样起来。

## [0.3.0] — 2026-09-21

### 新增

* **iPod 播放列表管理**。「iPod 音乐管理」页顶部加了「全部歌曲 / 歌单」切换，
  可以新建、改名、删除播放列表，把歌加进去、移出来。
  三条安全边界：主播放列表不可删/改名（iPod 的名字就存在它的标题里）、
  智能与播客播放列表只读并给出原因、**删除必须带真实预览过的一次性令牌**。
* 同步时按在线歌单在 iPod 上建同名播放列表（这条本来就有，这版把它和
  手动管理接上了）。

### 修复

* **普通歌单被克隆进播客数据集**：每写一次库就多一份重复歌单，界面上同一个
  歌单显示两条，改名/删除只动得掉其中一条——看起来像"删了没删掉"。
  根因是写库时 `podcast_playlists=None` 的语义是"把普通歌单克隆一份到播客
  数据集"（libgpod 新建库时的兼容行为），而这边只在"有非主播客歌单"时才传
  这个参数，设备上没有播客歌单是常态。现在只要设备有播客数据集就显式传。
* **改歌单会换掉它的身份**：写入器在没指定 id 时会生成一个新的。
  后果有两个——刚拿到的 id 立刻失效（加完歌再读这个歌单就是 404）；
  更麻烦的是**同步每跑一次，同名播放列表就换一个 id**，设备上按 id 引用
  它的东西（On-The-Go 等）就断了。现在同名替换继承原 id，改名也沿用。

### 安全

* **日志不再记录登录凭据**：新加的日志文件会把本地网易云服务打印的完整
  请求 URL 写进去，而那条 URL 上带着 `cookie=...`——那就是完整的登录凭证。
  现在转发日志时统一抹掉。它不该出现在日志里：用户报障会把日志发出来。

### 变更

* 版本号统一到 0.3.0。此前代码里写的是 0.1.0，而发行包叫 0.2.0，两边对不上。
* README 重写：548 行压到 190 行。原理、踩坑记录这类长内容本来就在 `docs/`
  里，首页只留"这是什么、怎么用、去哪看细节"。
* 隐私：文档和测试里的真机序列号、FireWire GUID、设备上的播放列表名，
  换成了明显是假的值。

## [0.2.0] — 2026-09-20

> **没有单独发布过 Release**。这个版本的改动随 0.3.0 一起发布；
> 当时代码里的版本号还写着 0.1.0（到 0.3.0 才统一）。

* **不需要装任何东西了**。CPython 运行时和网易云 API 所需的 Node 运行时
  都打进发行包：用户解压、双击，不用装 Python、不用装 node、不用联网初始化。
* 后端从"独立子进程"改为**嵌进桌面端同一个进程**，退出时不会有残留进程。
  开发时可以用 `IPOD_MANAGER_EXTERNAL_BACKEND=1` 退回外挂模式。
* 自带 node 加**看门狗**：主进程被强杀时，它会在 2 秒内自己退出，不留孤儿
  进程占着端口。
* 设备识别、写入流程的端到端验证脚本（`tools/verify_device_flow.py`），
  以及对一台真 iPod 做完整读写往返的彩排机制。

## [0.1.0] — 2026-09-20

首次发布。

* 把网易云歌单 /「我喜欢的音乐」下载到电脑，再同步进 **iPod Classic**
  （6th / 7th Gen，HASH58 签名）。
* 格式转换、标签与内嵌封面补全，**下载后比对时长揪出 30 秒试听片段**。
* 中文桌面端：歌单浏览、下载、iPod 音乐管理、设置（多账号登录与切换）。
* 命令行：`doctor` / `info` / `list` / `import` / `export` / `remove` /
  `verify` / `backup` / `restore` / `rename`。
* 设备写入前后各一道校验；备份 → 彩排 → 上真机的写入流程。
